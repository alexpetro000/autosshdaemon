{
  "name": "autosshdaemon",
  "version": "1.0.0",
  "description": "Persistent ssh tunnel daemon",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AlexP",
  "license": "ISC",
  "dependencies": {
    "autossh": "^0.2.0",
    "fs": "0.0.1-security",
    "request": "^2.88.0"
  }
}
